import 'package:flutter/cupertino.dart';

class NotBuiltYet extends StatelessWidget {
  const NotBuiltYet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("This hasn't been built yet");
  }
}
